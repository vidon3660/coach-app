.row.collapse
  = form_tag search_path do
    .ten.mobile-three.columns
      = search_field_tag "search", params[:search]
    .two.mobile-one.columns
      = button_tag "Search", :class => 'button expand postfix'