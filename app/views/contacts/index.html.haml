.row
  .six.columns
    %h1 Friends
    %ul
      - @contacts.each do |contact|
        %li= link_to contact.name, player_path(contact)

  .six.columns
    %h1 Trainings
    %ul
      - @contacts.each do |contact|
        %li= link_to contact.name, player_path(contact)